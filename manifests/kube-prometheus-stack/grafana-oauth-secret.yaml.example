# Grafana OAuth Secret
# This secret stores the Authentik OAuth client secret for Grafana
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to grafana-oauth-secret.yaml:
#    cp grafana-oauth-secret.yaml.example grafana-oauth-secret.yaml
#
# 2. After Authentik is deployed, retrieve the auto-generated client secret:
#    - Log into Authentik UI at https://authentik.local
#    - Navigate to: Applications → Providers → Grafana
#    - Click "View" to see the client secret
#
# 3. Replace AUTHENTIK_CLIENT_SECRET_HERE with the actual value
#
# 4. This secret will be automatically deployed on boot via Containerfile
#
# NOTE: grafana-oauth-secret.yaml is in .gitignore - never commit secrets to git!

apiVersion: v1
kind: Secret
metadata:
  name: grafana-oauth-secret
  namespace: monitoring
type: Opaque
stringData:
  # Replace with actual client secret from Authentik
  client_secret: "AUTHENTIK_CLIENT_SECRET_HERE"
